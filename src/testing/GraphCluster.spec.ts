// import { createNodeString } from "../components/GraphVizStrings";
// import {
//     createCluster,
//     createClusterString,
// } from "../core/graphElements/GraphCluster";

test.skip("test a string is returned", () => {
    // const node = createNode("string");
    // expect(node.name).toEqual("string");
    expect(1).toBe(1);
});
